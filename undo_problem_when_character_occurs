#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
using namespace std;
int main()
{ int cnt=0;
vector<string> tu;
string str;
cin>>str;
int n=str.length();
for(int i=0;i<n;i++){
cnt = count(str.begin(),str.end(),str[i]);
tu.push_back(string(1, str[i]));
tu.push_back(to_string(cnt));
}
return 0;
}
