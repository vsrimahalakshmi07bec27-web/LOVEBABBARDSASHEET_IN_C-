class Solution {
public:
    int stockBuySell(vector<int> &arr) {
        int mp=0;
        for (int i=1;i<arr.size();i++) {
            if (arr[i]>arr[i-1]) {
                mp+=(arr[i]-arr[i-1]);
            }
        }
        return mp;
    }
};

